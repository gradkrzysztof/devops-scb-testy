
name: "04 – Using Actions"

on: 
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    defaults:
      run:
        working-directory: cwiczenia/react-app
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Printing Folders
        run: |
          ls -la
          pwd